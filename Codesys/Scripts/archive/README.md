# Archived CODESYS scripts

One-shot probes, already-applied project patches, and `PLCOpenExport.py` (Prepare/Cut already export XML). Kept for history; not part of the daily or release workflow.

To run one, point `RunCodesysScript.py --script` at the file in this folder. Patch scripts that load ST look for `archive/st/` next to themselves.
